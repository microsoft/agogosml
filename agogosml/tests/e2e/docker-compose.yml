version: "3.7"

services:
  tester-gen:
    build: testgen/.
    env_file: tester.env
    ports:
     - "127.0.0.1:2000:5000"
    networks:
      mynet:
        aliases:
          - tester

networks:
  mynet: