apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-pipeline-app.fullname" . }}
  labels:
    app: {{ include "data-pipeline-app.name" . }}
    chart: {{ include "data-pipeline-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "data-pipeline-app.name" . }}
    release: {{ .Release.Name }}
